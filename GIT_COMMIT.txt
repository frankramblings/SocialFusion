feat(composer): implement muscle memory composer features and fixes

Implement comprehensive power-user composer features including entity
compilation, platform conflict detection, keyboard shortcuts, smart paste,
emoji fetching, and cache persistence. Fix critical entity range maintenance
and undo/redo integration issues.

Features:
- Entity parsing from plain text (mentions, hashtags, URLs)
- Entity range maintenance via applyEdit() callback
- Undo/redo integration for entity state sync
- Smart paste detection with automatic entity creation
- Platform conflict detection with automatic updates
- Keyboard shortcuts (Cmd+Enter, Cmd+K, Cmd+L, Cmd+.)
- Full AutocompleteCache persistence to UserDefaults
- Mastodon custom emoji fetching + system emoji fallback

Build fixes:
- Fix keyboard shortcut API (onKeyPress for iOS 17+)
- Fix type-checking complexity
- Fix String indexing errors
- Fix parameter order issues

Files modified:
- ComposerTextModel.swift: Add entity parsing
- ComposeView.swift: Wire up callbacks, add paste detection
- ComposeViewLifecycleModifier.swift: Fix keyboard shortcuts
- AutocompleteCache.swift: Full persistence implementation
- EmojiService.swift: Mastodon API + system emoji

Build verified for iPhone 17 Pro simulator.
