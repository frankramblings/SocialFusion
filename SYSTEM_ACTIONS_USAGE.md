# System Actions Usage

Use `PostSystemActions` or the `Post` convenience methods:

```swift
post.openInBrowser()
post.copyLink()
post.presentShareSheet()
post.openAuthorProfile()
```

These helpers provide haptic feedback and handle iPad share sheet presentation safely.
