/*
// boost banner
if let boostedBy = post.boostedBy {
    BoostBanner(handle: boostedBy)
}

// reply + parent
if let parent = post.parent {
    // always show pre-loaded parent
    ReplyBanner(handle: parent.authorHandle)
    TimelineCard {
        ParentPostPreview(post: parent)
    }
}
*/
